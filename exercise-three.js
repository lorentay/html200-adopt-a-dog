// Challenge 3: An Array with a Loop


let whiteFlowers = ['Hydrangea', 'Mandevilla', 'Camellia', 'Impatiens', 'Rose'];

// Loops through the array
for (let i = 0; i < whiteFlowers.length; i++) {
  
// Console logs the index (starting at 1), comma, and the flower
console.log(`${i + 1}, ${whiteFlowers[i]}`);
}
